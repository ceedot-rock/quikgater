{
  "name": "fetchgate-browser-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Fly.io render/hard-fallback service for Fetchgate (spec §4, Layers 2-3).",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.7.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
